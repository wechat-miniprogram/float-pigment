node_modules/
target/

# napi-rs build artifacts: the compiled addon is never committed.
# index.js (loader) and index.d.ts (types) ARE committed — they are
# stable, platform-independent, and regenerated by `napi build`.
*.node

# native addons are copied into per-platform npm dirs at publish time
npm/**/*.node
